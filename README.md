## 📂 Database Setup

A pre-configured MySQL database dump is included:

📄 the database file: yen_sao.sql

### To import it:

1. Open **Laragon**, XAMPP, or your preferred MySQL tool.
2. Create a database with the name dtvietnam
3. Import the SQL file:

## 🔧 Local Development Setup

To run the project locally, open **two terminals** and run:

1. php artisan serve - for laravel
2. npm run dev - for vite
